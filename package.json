{
  "name": "gif-windstorm",
  "version": "1.0.0",
  "description": "implementation of catastrophic insurance product using gif framework",
  "scripts": {
    "test": "truffle test",
    "premigrate": "mkdir -p ./build/contracts/ && cp -R ./lib/artifacts/ ./build/contracts/",
    "migrate": "truffle migrate",
    "migrate:reset": "truffle migrate --reset"
  },
  "author": "Joel Martínez @j0x0j",
  "license": "ISC",
  "devDependencies": {
    "@etherisc/gif": "^1.0.0",
    "date-fns": "^1.30.1",
    "dotenv": "^6.2.0",
    "truffle": "^5.0.24",
    "truffle-assertions": "^0.9.1"
  },
  "dependencies": {}
}
